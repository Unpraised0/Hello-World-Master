//Unpraised.

#include <stdio.h> // ใช้สำหรับนำเข้าโค้ดจากไฟล์อื่นเข้ามาใช้งานในโปรแกรม

int main(){
    printf("Hello World."); // คำสั่ง printf ใช้สำหรับพิมพ์ข้อมูล ข้อมูลจากตัวแปรออกสู่หน้าจอ (Output)
    getchar();// รอรับอินพุตจากผู้ใช้ จากนั้นก็จะหยุดการทำงานไป
    
    return 0; // ส่งค่ากลับจากโปรแกรมที่ทำงานสมบูรณ์ (successful termination) หรือแสดงว่าโปรแกรมทำงานได้ถูกต้องและไม่มีปัญหาใดๆในการทำงาน.
 }
