//Unpraised.

#include <stdio.h> // #include <header_file> ใช้สำหรับนำเข้าโค้ดจากไฟล์อื่นเข้ามาใช้งานในโปรแกรม

int main(){
    printf("Hello World."); // printf ( "formatted_string", arguments_list) ใช้สำหรับพิมพ์ข้อมูล ข้อมูลจากตัวแปรออกสู่หน้าจอ (Output)
    getchar();// getchar(void) รอรับอินพุตจากผู้ใช้
    
    return 0; // ส่งค่ากลับจากโปรแกรมที่ทำงานสมบูรณ์ (successful termination) หรือแสดงว่าโปรแกรมทำงานได้ถูกต้องและไม่มีปัญหาใดๆในการทำงาน.
 }
